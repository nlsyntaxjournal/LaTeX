# LaTeX

Hello, read this.

Better yet, read syntaxtemplate.tex, especially the comments in the preamble.

Choose the version of the .sty file that is appropriate for the compiler your document needs: pdftex (the default compiler in most installations), TeX and DVI, or XeTeX.
